# Keep peer-dependency resolution lenient across the RN / Expo / Next matrix.
# The workspaces intentionally span React versions that these tools reconcile
# themselves; strict peer installs cause false failures.
legacy-peer-deps=true
save-exact=false
fund=false
audit=false
